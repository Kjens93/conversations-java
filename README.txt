System requirements:
To run: JRE 1.8 and Maven 3.3.x must be installed.
To compile from source: Project Lombok plugin for your preferred IDE must be installed.

To run tests, execute the following command from the source root:
mvn compile && mvn test
